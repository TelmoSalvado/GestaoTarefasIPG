@model IEnumerable<GestãoTarefasIPG.Models.Cargos>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nível)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nível)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CargosID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CargosID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CargosID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
